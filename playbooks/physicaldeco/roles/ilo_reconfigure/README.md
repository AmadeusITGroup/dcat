deploy_hp
=========

This role installs the hp-ams and hponcfg disables snmpd and removes the obsolete hp packages

Requirements
------------

FOP-offline repository has to be configured.

Role Variables
--------------

No variables are configured.

Dependencies
------------


Example Playbook
----------------

Including an example of how to use your role (for instance, with variables passed in as parameters) is always nice for users too:

    - hosts: servers
      roles:
         - { role: deploy_hp }

License
-------

BSD

