---
    - name: "Check entry for DECO server for filter host list cmdb fetch bgp"
      throttle: 1
      block:
 
        - set_fact:
            host_list: host_list_cmdb_fetch_bgp.yml

        - name: Display the JSON file content
          shell: cat record_healthcheck.json
          delegate_to: localhost
          register: TR_result_healthcheck  

        - name: save the Json data to a Variable as a Fact
          set_fact:
            record_id_healthcheck: "{{ TR_result_healthcheck.stdout[1:-1] }}"
          delegate_to: localhost

        - meta: end_host
          when: record_id_healthcheck != ""
                
        - name: Display the JSON file content
          shell: cat private_endpoint_connection_string.yml
          delegate_to: localhost
          register: result_private_endpoint_connection_string
    
        - name: save the Json data to a Variable as a Fact
          set_fact:
            private_endpoint_connection_string: "{{ result_private_endpoint_connection_string.stdout }}"
          delegate_to: localhost      
                
        - name: "Check entry for DECO server for filter hostlist for cmdb duplicate list"
          azure_update_ref_esxi:
            connection_string: "{{ private_endpoint_connection_string }}"
            hostname: "{{ inventory_hostname }}"
          register: idem_status 
          delegate_to: localhost   

        - set_fact: 
            cmdb_fetch_bgp: '{{idem_status.module_update.cmdb_fetch}}'                   
           
        - set_fact: 
            fqdn_info: '{{idem_status.module_update.fqdn_info }}'

        - set_fact: 
            vcenter_info: '{{idem_status.module_update.vcenter_info }}'

        - name: "physical server list qualified for cmdb status check bgp"
          shell: echo "{{ inventory_hostname }}," >> {{ host_list }}                   
          delegate_to: localhost
          when: cmdb_fetch_bgp == "F" and fqdn_info != "F" and vcenter_info != "F"              

            
      rescue:
        - debug:
            msg: "Failed to fetch hostlist for CMDB status check bgp"      

