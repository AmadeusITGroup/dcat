---
# - name: "This playbook is to Create TR for service health check SSH"
#   hosts: localhost
#   gather_facts: no

#   vars:
#     graceperiod_flag: False  
           
      #### TR creation  #####  
      
    
  # tasks:    
    - name: "Handling Create TR- Win@proach for health check task "
      block:


        - name: Display the JSON file content
          shell: cat vcenter_host_facts_fail.yml
          delegate_to: localhost
          register: result_check_maint

        - name: save the Json data to a Variable as a Fact
          set_fact:
            esxi_maint_fail: "{{ result_check_maint.stdout }}" 
          delegate_to: localhost                                                                    

        - meta: end_host
          when: esxi_maint_fail == ""     


        - name: "Custom module [CREATE TR]"
          create_tr:
            record_type: "TR"
            title: "CSTY:DECO:DCAT: ESXI RACK DECO : Service health check - Maintenance Mode"
            assignee_name: "DecoP112686"
            assignee_group: "DINFDECO"            
            severity: "3"
            overview: "ESXI DECO - Service health check - Maintenance Mode"
            parent_id: "{{ cr }}"           
            username: "DecoP112686"     
            password: "opFCtCpIMzmC"
          delegate_to: localhost  
          no_log: False
          register: create_tr_output          

        - set_fact:
            record_id: "{{create_tr_output.record_id}}"
   
    
        - copy:
            content: '{{ record_id|to_json }}'
            dest: record_healthcheck_maint.json
          delegate_to: localhost

        - name: adding documentation tag
          tr_documentation:
            username: "{{ winaproach_username }}"    
            password: "{{ winaproach_password }}"
            win_url: "{{win_url}}"
            task_id: "{{ record_id }}"
          register: add_document
          delegate_to: localhost

      rescue:
        - debug:
            msg: "Failed to Create ESXI DECO Maintenance - TR"

    - name: "Handling Create TR- Win@proach for health check task - Retry 1"
      block:                                                  

        - name: Display the JSON file content
          shell: cat vcenter_host_facts_fail.yml
          delegate_to: localhost
          register: result_check_maint

        - name: save the Json data to a Variable as a Fact
          set_fact:
            esxi_maint_fail: "{{ result_check_maint.stdout }}" 
          delegate_to: localhost 
                                                          

        - meta: end_host
          when: esxi_maint_fail == ""    


        - name: Display the JSON file content
          shell: cat record_healthcheck_maint.json
          delegate_to: localhost
          register: TR_result_healthcheck  

        - name: save the Json data to a Variable as a Fact
          set_fact:
            record_healthcheck_maint: "{{ TR_result_healthcheck.stdout[1:-1] }}"
          delegate_to: localhost

        - name: Sleep for 20 seconds and continue with play
          ansible.builtin.wait_for:
            timeout: 20
          delegate_to: localhost
          when: record_healthcheck_maint == ""                          

        - name: "Custom module [CREATE TR]"
          create_tr:
            record_type: "TR"
            title: "CSTY:DECO:DCAT: ESXI RACK DECO : Service health check - Maintenance Mode"
            assignee_name: "DecoP112686"
            assignee_group: "DINFDECO"            
            severity: "3"
            overview: "ESXI DECO - Service health check - Maintenance Mode"
            parent_id: "{{ cr }}"            
            username: "DecoP112686"     
            password: "opFCtCpIMzmC"
          delegate_to: localhost  
          no_log: False
          register: create_tr_output   
          when: record_healthcheck_maint == ""       

        - set_fact:
            record_id: "{{create_tr_output.record_id}}"
          when: record_healthcheck_maint == ""    

    
        - copy:
            content: '{{ record_id|to_json }}'
            dest: record_healthcheck_maint.json
          delegate_to: localhost
          when: record_healthcheck_maint == ""

        - name: adding documentation tag
          tr_documentation:
            username: "{{ winaproach_username }}"    
            password: "{{ winaproach_password }}"
            win_url: "{{win_url}}"
            task_id: "{{ record_id }}"
          register: add_document
          delegate_to: localhost
          when: record_healthcheck_maint == ""

      rescue:
        - debug:
            msg: "Retried and Failed to Create ESXI DECO Maintenance - TR"

    - name: "Handling Create TR- Win@proach for health check task-Retry 2 "
      block:                                                  

        - name: Display the JSON file content
          shell: cat vcenter_host_facts_fail.yml
          delegate_to: localhost
          register: result_check_maint

        - name: save the Json data to a Variable as a Fact
          set_fact:
            esxi_maint_fail: "{{ result_check_maint.stdout }}" 
          delegate_to: localhost 
                                                                     

        - meta: end_host
          when: esxi_maint_fail == ""     


        - name: Display the JSON file content
          shell: cat record_healthcheck_maint.json
          delegate_to: localhost
          register: TR_result_healthcheck  

        - name: save the Json data to a Variable as a Fact
          set_fact:
            record_healthcheck_maint: "{{ TR_result_healthcheck.stdout[1:-1] }}"
          delegate_to: localhost

        - name: Sleep for 20 seconds and continue with play
          ansible.builtin.wait_for:
            timeout: 20
          delegate_to: localhost
          when: record_healthcheck_maint == ""    
          
        - name: "Custom module [CREATE TR]"
          create_tr:
            record_type: "TR"
            title: "CSTY:DECO:DCAT: ESXI RACK DECO : Service health check - Maintenance Mode"
            assignee_name: "DecoP112686"
            assignee_group: "DINFDECO"            
            severity: "3"
            overview: "ESXI DECO - Service health check - Maintenance Mode"
            parent_id: "{{ cr }}"            
            username: "DecoP112686"     
            password: "opFCtCpIMzmC"
          delegate_to: localhost  
          no_log: False
          register: create_tr_output 
          when: record_healthcheck_maint == ""
       

        - set_fact:
            record_id: "{{create_tr_output.record_id}}"
          when: record_healthcheck_maint == ""  
  
    
        - copy:
            content: '{{ record_id|to_json }}'
            dest: record_healthcheck_maint.json
          delegate_to: localhost
          when: record_healthcheck_maint == ""

        - name: adding documentation tag
          tr_documentation:
            username: "{{ winaproach_username }}"    
            password: "{{ winaproach_password }}"
            win_url: "{{win_url}}"
            task_id: "{{ record_id }}"
          register: add_document
          delegate_to: localhost
          when: record_healthcheck_maint == ""

      rescue:
        - debug:
            msg:  "Retried and Failed to Create ESXI DECO Maintenance - TR"                              

