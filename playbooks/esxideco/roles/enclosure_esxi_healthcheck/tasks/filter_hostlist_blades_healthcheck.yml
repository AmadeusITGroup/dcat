---
    - name: "Filter hostlist for Blades Health Check"
      throttle: 1
      block:
 
        - set_fact:
            host_list: host_list_blades_health_check.yml
          run_once: true


        # - name: Display the JSON file content
        #   shell: cat record_healthcheck.json
        #   delegate_to: localhost
        #   register: TR_result_healthcheck  

        # - name: save the Json data to a Variable as a Fact
        #   set_fact:
        #     record_id_healthcheck: "{{ TR_result_healthcheck.stdout[1:-1] }}"
        #   delegate_to: localhost

        # - meta: end_host
        #   when: record_id_healthcheck != ""

        # - name: Display the JSON file content
        #   shell: cat esxi_ssh_fail.yml
        #   delegate_to: localhost
        #   register: TR_result_esxi_ssh_fail  

        # - name: save the Json data to a Variable as a Fact
        #   set_fact:
        #     ssh_fail: "{{ TR_result_esxi_ssh_fail.stdout }}"
        #   delegate_to: localhost

        # - meta: end_host
        #   when: ssh_fail is search("{{ inventory_hostname }}")    

        - name: Display the JSON file content
          shell: cat private_endpoint_connection_string.yml
          delegate_to: localhost
          run_once: true
          register: result_private_endpoint_connection_string
    
        - name: save the Json data to a Variable as a Fact
          set_fact:
            private_endpoint_connection_string: "{{ result_private_endpoint_connection_string.stdout }}"
          delegate_to: localhost      
          run_once: true

                
        - name: "Check entry for DECO server for filter hostlist for blade healthcheck"
          azure_update_ref_esxi.py:
            connection_string: "{{ private_endpoint_connection_string }}"
            hostname: "{{inventory_hostname}}"
          register: idem_status 
          delegate_to: localhost

        # - set_fact: 
        #     healthy: '{{idem_status.module_update.healthy}}'  
                 
        - set_fact: 
            server_shutdown: '{{idem_status.module_update.server_shutdown}}'                  
           
        - name: "esxi server list qualified for blades healthcheck"
          shell: echo "{{ inventory_hostname }}," >> {{ host_list }}                   
          delegate_to: localhost
          # when: healthy == "T" and cmdb_dup_check == "F"      
          when: server_shutdown == "F"                 
           
           
      rescue:
        - debug:
            msg: "Failed to fetch hostlist for blades health check "      

