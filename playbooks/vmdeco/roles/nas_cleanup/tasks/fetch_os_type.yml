#   tasks:
    - name: Handle the error-Fetching os type of the servers
      throttle: 1
      block:

        - name: Display the JSON file content
          shell: cat private_endpoint_connection_string.yml
          delegate_to: localhost
          register: result_private_endpoint_connection_string
    
        - name: save the Json data to a Variable as a Fact
          set_fact:
            private_endpoint_connection_string: "{{ result_private_endpoint_connection_string.stdout }}"
          delegate_to: localhost

        - name: "Check entry for DECO server"
          azure_update_ref:
            connection_string: "{{ private_endpoint_connection_string }}"
            hostname: "{{inventory_hostname}}"
          register: idem_status
          delegate_to: localhost
      

        - set_fact: 
            os: '{{idem_status.module_update.os}}'       

        - name: awx creds username to var
          set_fact:
            pAccount_user: "{{ paccount_username }}"
          delegate_to: localhost

        - name: awx creds password to var
          set_fact:
            paccount_password: "{{ paccount_password }}"
          delegate_to: localhost

        - name: "Gather OS information associated with the given VM"
          vmware_guest_info:
            hostname: '{{ v_center_gp }}'
            username: "{{ pAccount_user }}"
            password:  "{{ paccount_password }}"
            name: "{{ vm_deletion_name }}"      
            validate_certs: no
            datacenter: ""
          delegate_to: localhost            
          register: nas_vmfacts
          when: os == "F"

        - set_fact:
            nas_os_detail_result: '{{ nas_vmfacts.instance.hw_guest_full_name }}'
          when: os == "F"

        - set_fact:
            nas_os_detail_w: "windows"
          when: os == "F" and nas_os_detail_result is search("Windows")

        - set_fact:
            nas_os_detail_w: "linux"
          when: os == "F" and nas_os_detail_result is search("Linux|CentOS")

        - set_fact:
            nas_os_detail_w: "windows"
          when: os is search("windows")

        - set_fact:
            nas_os_detail_w: "linux"
          when: os is search("linux")
           
             
      rescue:
        - name: when above one throws error the below task should execute
          debug:
            msg: "Exception occurred but rescued - check errors for os type fetch module"

        - set_fact:
            vm_fetch_os_msg: "Exception occurred while fetching OS type of server for NAS storage cleanup. Kindly check fetch_os_type module in NAS storage"

        - name: Get timestamp from the system
          shell: "date +%Y-%m-%d/%H:%M"
          delegate_to: localhost
          register: tstamp              
                  
        - set_fact:
            time: "{{tstamp.stdout}}"

        - name: "Write Failure host to file"
          shell: echo "{{ inventory_hostname }} Failure" >> vm_nas_hostname_failure.txt                    
          delegate_to: localhost

        - name: "VM host list"
          shell: echo "{{ inventory_hostname }}, {{ time }}, [ERROR], Exception occurred while fetching OS type of server from vcenter either connection issue to vcenter or server not found . Kindly check fetch_os_type or vcenter fetch module in NAS storage."  >> {{file_failure}}                    
          delegate_to: localhost